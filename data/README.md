# Data

원본 분석 데이터는 저장소에 포함하지 않습니다.

실행하려면 이 폴더에 `dementia_data.csv`를 두세요.

필수 조건:
- `target` 컬럼 포함
- 나머지 컬럼은 모델 입력 피처

```text
data/
├── README.md
└── dementia_data.csv   # local only / gitignored
```
